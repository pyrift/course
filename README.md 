# Course Project

A simple Django web application that collects user information (age, thoughts, phone, Telegram, etc.) with many-to-many relationships to Branches and Burning directions.

## Features

- Create and manage `Branch` and `Burning` categories
- Collect user submissions via backend data (currently via admin)
- Filter submissions by `Branch` (e.g., "backend") or `Burning` (e.g., "mobile")
- Display results in a clean HTML table
- PostgreSQL database support
- Environment-based configuration (`.env`)
- Admin panel for data management

## Tech Stack

- Python 3.12
- Django 6.0
- PostgreSQL
- HTML/CSS (basic table layout)

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/pyrift/course.git
   cd course